<?php

namespace App\Repositories;

interface RepositoryInterface
{
    // Here we could define some methods like create, update, all etc
    // but for this project we don't need them.
    //
    // If we add more tables to the database, we'll consider defining common methods or removing this interface.
}
